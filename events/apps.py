from django.apps import AppConfig


class EventsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'events'

class EventsConfig(AppConfig):
      name = 'events'
      verbose_name = "French Flying Club"

      